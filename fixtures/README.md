# Test Fixtures

Fixtures must be synthetic, bounded, reviewed for secrets, and owned by the
feature whose behavior they protect.

Protocol, terminal, SSH, path, and security fixture directories are introduced
with the milestone that first uses them.
